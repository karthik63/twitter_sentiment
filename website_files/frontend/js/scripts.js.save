// Empty JS for your own code to be here



$(document).ready(function() { 


function setValue() {
	para = document.getElementById("tweet-text-body");
	
        input_value = @app.route('/receiver', methods = ['POST'])
def worker():
        # read json + reply
        data = request.get_json()
        result = ''

        for item in data:
                # loop over every row
                result += str(item['make']) + '\n'

        return result

        $.post("receiver", "{["a", "b", "c"]}", function(){

	});

	event.preventDefault();

	
}

 });


